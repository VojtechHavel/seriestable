const API_URL = "http://localhost:8110";

console.log("process.env.NODE_ENV", process.env.NODE_ENV);

// if(process.env.NODE_ENV !== 'development'){
//     API_URL = "https://api.videomark.app";
// }

export {API_URL}